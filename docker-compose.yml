services:
  tracktorr:
    build: .
    container_name: tracktorr
    restart: unless-stopped
    ports:
      - "3030:3030"
    volumes:
      - ./config/config.json:/app/config/config.json:ro
      - tracktorr-data:/data

volumes:
  tracktorr-data:
